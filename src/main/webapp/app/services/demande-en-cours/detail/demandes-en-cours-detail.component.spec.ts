import {ComponentFixture, TestBed} from '@angular/core/testing';

import {DemandesEnCoursDetailComponent} from './demandes-en-cours-detail.component';

describe('User Management Detail Component', () => {
  let comp: DemandesEnCoursDetailComponent;
  let fixture: ComponentFixture<DemandesEnCoursDetailComponent>;


  beforeEach(() => {
    fixture = TestBed.createComponent(DemandesEnCoursDetailComponent);
    comp = fixture.componentInstance;
  });
});
